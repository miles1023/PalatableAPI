# RE Session — 2026-05-15

## Focus
Project scaffolding and initial knowledge base population from online sources.

## Sources Used
- PalSchema GitHub (Okaetsu) — C++ loader headers, example JSONs, schema files
- NightFyre/Palworld-Internal — C++ SDK class hierarchy and property access patterns
- mczubaj/Palworld-Mods — UE4SS Lua hook paths
- DRayX CXX Header Gist — Full enum dump (300+ enums)
- PalworldDataTools/PalworldDataExtractor — Struct property names
- Elliesaur/Palworld-Mods — FindAllOf patterns, dynamic item data
- pwmodding.wiki — Tool workflows and UE4SS docs

## Populated This Session
- 11 entity YAML files (APalPlayerCharacter, APalPlayerController, APalPlayerState,
  UPalCharacterParameterComponent, UPalIndividualCharacterParameter,
  UPalPlayerInventoryData, UPalItemContainer, UPalItemSlot,
  UPalCharacterMovementComponent, APalCharacter, base classes)
- 7 DataTable YAML files (DT_PalMonsterParameter with all 55 columns,
  DT_PalDropItem, DT_ItemRecipeDataTable, DA_StaticItemDataAsset,
  DT_WazaDataTable stub, DT_WazaMasterLevel, DT_BuildObjectDataTable)
- hooks.yml — all confirmed UE4SS hook/notify paths
- enums.yml — all confirmed enum values including EPalTribeID partial map
- 37 plain-English API commands mapped to underlying data sources

## Key Architecture Decisions Made
- Two-tier property access: reflected (UE4SS name-based) vs direct (raw offset fallback)
- Two execution backends: runtime (UE4SS Lua hooks) vs datatables (PalSchema JSON patches)
- UE4SS as the DLL injection foundation — we build on top of it
- YAML files are living documents — every session adds new confirmed data

## Key Open Questions Logged
- DT_WazaDataTable column names are unconfirmed — needs FModel export
- Full EPalTribeID to plain-English name map is ~60% complete — needs remaining 60+ entries
- FFixedPoint inner field name needs confirmation (.Value or .RawValue?)
- Server binary hook paths are largely unknown — all addresses say null for server

## Next Session Priorities
1. Run FModel with Mappings.usmap to export DT_WazaDataTable column names
2. Run UE4SS UHT generator in-game to get current property names and confirm all .yml entries
3. Begin CheatEngine live session to confirm FFixedPoint field access pattern
4. Complete EPalTribeID plain-English name map from PalSchema example mods
5. Find and document level-up, death, and capture hook paths
